[package]
name = "ducktape-rpc"
edition.workspace = true
version.workspace = true
publish = false

[dependencies]
futures.workspace = true
reqwest.workspace = true
serde.workspace = true
# the digest a signed write proves itself over — computed here because the
# streaming upload never holds the body for anyone else to hash.
sha2.workspace = true
serde_json.workspace = true
tokio.workspace = true
tokio-tungstenite = "0.24"

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
