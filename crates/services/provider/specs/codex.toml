# Capability spec: OpenAI Codex CLI.
#
# This file is EMBEDDED in the provider crate as a built-in default and
# parsed by the exact same code path as operator-provided specs — it is the
# reference example of the format. An operator spec with `tag = "codex"` in
# the workspace's capability dir overrides this file entirely (no field merging);
# each [[variants]] tag below is its own tag and is overridden the same way.
#
# Which MODEL codex runs is host policy, not consensus data. The BASE tag
# ("codex") passes no --model flag, so the CLI uses whatever the operator
# configured in codex itself. The [[variants]] below are the curated
# model/effort matrix: each one is load-time expansion into a finer tag
# `codex_<model>_<effort>` with its own fully literal argv (no placeholders,
# no dispatch-time routing) — exactly as if each were its own spec file.
#
# Agentic posture: `sandbox_mode="workspace-write"` lets the CLI edit files in its
# working directory — which is the workspace the run's provisioner
# materialized for this attempt, so edits never touch the node's data dir.
# Every run starts cold: a run's whole continuity is the prompt envelope (full
# transcript), which is what makes a run portable across assignees.
#
# Format reference: docs/records/specs/capability-spec.md

spec = 1

[capability]
tag = "codex"
description = "OpenAI Codex CLI, managed App Server session (BYO auth: the CLI manages its own login)"

[detect]
# Probed on PATH; DUCKTAPE_CODEX_BIN points at an explicit binary instead
# (an explicit override that does not exist is a loud warning + absent
# capability, never a silent fallback to PATH).
bin = "codex"
env = "DUCKTAPE_CODEX_BIN"
# Current Codex releases resolve Code Mode through this sibling executable.
# It is required at discovery and mounted beside `codex` in sandbox guests;
# declaring it here keeps executor-specific package knowledge out of Rust.
companions = ["codex-code-mode-host"]

[source]
# Where `ducktape agent install codex` gets the guest's Linux build: the
# LATEST GitHub release, resolved at install time — no version is written
# down anywhere. `asset` is the release artifact for the guest's arch
# ({arch} = x86_64 | aarch64): the `codex-package` tar carries `bin/codex`
# AND its Code Mode companion in one artifact the release publishes a
# checksum for in `sums` (the bare `codex-<triple>` asset ships neither).
# `members` are the archive paths of exactly the files [detect] declares.
kind = "github-release"
repo = "openai/codex"
asset = "codex-package-{arch}-unknown-linux-musl.tar.gz"
sums = "codex-package_SHA256SUMS"
members = ["bin/codex", "bin/codex-code-mode-host"]

[invoke]
# Bidirectional App Server stdio; the host sends the prompt with turn/start.
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\""]
prompt = "stdin"
# The IDLE budget (output refreshes it; hard-capped at 36x). App Server
# streams an event per action, so a long agentic run refreshes continuously
# and only a genuinely STALLED run dies at this window. Operators override
# per tag by shadowing the spec, or globally via
# DUCKTAPE_PROVIDER_TIMEOUT_SECS.
timeout_secs = 600

[output]
# App Server events, including input acknowledgements and approval requests.
format = "codex-session"

[interactive]
# The pty-backed TUI argv, for interactive sessions (a member drives the codex
# TUI directly), as opposed to the headless [invoke] App Server above. Empty
# args = a bare `codex` launch, which opens the TUI. The broker's `-c` overrides
# (model_providers.ducktape + model_provider + workspace trust) are PREPENDED
# host-side — codex reads `-c` as global config — so the SAME isolation applies:
# the credential stays in the host broker, the child gets only the opaque bearer
# and loopback base URL, and the fresh CODEX_HOME below blocks any dotfile
# fallback. NOTE (PR1 scope): interactive sessions do NOT wire the [tools] MCP
# plane — a human at the TUI needs no autonomous session-key tool door. The
# exact TUI invocation is pending live validation against the real codex binary
# (see the design spec).
args = []
# The RESTRICTED argv for a SHARED (command-lane) session: read-only + never
# ask. A shared session is driven by ordered commands, so no single member is
# there to answer a TUI approval prompt — codex must never stop to ask, and
# read-only makes not-asking safe (it can read + reason + reply, never write or
# exec). `-c` broker overrides are still prepended host-side, so the same
# credential isolation holds. Absent this key, a capability is solo-only.
restricted_args = ["--sandbox", "read-only", "--ask-for-approval", "never"]

[isolation]
# THE STRONG AUTH PATH: the operator's credential never enters the codex
# process. The HOST reads it (OPENAI_API_KEY, else ~/.codex/auth.json) and holds
# it in the node; a per-run loopback Responses broker serves the model API, and
# codex gets only an opaque random bearer plus a 127.0.0.1 base URL, both of
# which die with the run. Neither can recover the credential.
#
# config_home_env is what MAKES that true. Pointed at a fresh, EMPTY CODEX_HOME
# under the run's workdir, codex cannot read the operator's ~/.codex/auth.json
# and must use the broker the argv names. Without it the broker would be
# decorative — codex would just log in from the dotfile as before.
#
# There is no way to mount ~/.codex in instead: the spec format has no
# [sandbox] table, and a run's microVM shares no filesystem with the host. The
# guest reaches the host-side broker because duck-guest-init serves its port on
# the VM's own loopback and tunnels it over vsock.
# Inherited by every [[variants]] tag below.
config_home_env = "CODEX_HOME"
broker = "codex-responses"

[context]
# WHERE THE AGENT'S SOUL LANDS. The host assembles the agent's curated skills
# into one markdown document per run; this names the file THIS CLI already
# auto-loads it from, so no Rust ever branches on an executor name. codex reads
# $CODEX_HOME/AGENTS.md as its global instructions (verified live), and
# [isolation] above already gives every run a fresh, empty CODEX_HOME — so the
# doc lands in a directory only this run can see.
#
# That directory sits under the workdir's reserved .ducktape-run/ dir, which the
# provisioner deletes before duckfs/forge scan the tree: the soul can never
# become an agent artifact, and it never collides with a repository's OWN
# AGENTS.md (which stays inside the checkout and layers on top of this one).
# It crosses for free — the workdir rides the run's workspace device.
# Inherited by every [[variants]] tag below.
path = "config-home:AGENTS.md"

[tools]
# WHICH MCP SYNTAX this CLI is handed the run's tool plane in — not an argv.
# The endpoint is a loopback address on the run's OWN node tunnel, drawn when
# its lane binds, so it cannot be written down in a file: the host composes
# `-c mcp_servers.ducktape.url=…` per run (provider_host::mcp_argv), the same
# way the credential broker's `-c` overrides are composed, and the pre-approval
# rides with it — without it `exec` cancels every ducktape call rather than
# asking a human who is not there.
#
# This reaches EVERY argv this file produces: the base tag, every [[variants]]
# tag, and the [interactive] TUI argv above, because the host wires it where
# the run boots rather than where the file is parsed.
#
# Nothing about the tool plane enters the guest — no server command, no
# `ducktape` binary on its PATH, no run-scoped token in its environment. The
# run reaches the catalog over the one tunnel it already has, and the node
# knows which run is calling because the lane IS that run.
#
# Ducktape MCP tools remain preapproved under the committed run grant.
# Shell and file approval requests go to the run creator in Agents.
mcp = "codex"

# ---- curated model/effort matrix -------------------------------------------
# The models the codex CLI itself lists (0.144.1): the gpt-5.6 family plus
# gpt-5.5. Each model carries only the efforts that model actually supports —
# 5.6 goes up to `max`, 5.5 stops at `xhigh` — so no tag promises a run the
# CLI would reject. (`ultra` is deliberately absent: it delegates subtasks,
# which does not belong in a one-shot non-interactive exec.) Retiring a model
# here retires its tags; an operator keeping an older one shadows it with
# their own spec file.
#
# Each variant pins its model and reasoning effort with -c. The
# -c value deliberately carries embedded double quotes ('...="low"'): codex
# parses the value as TOML, and argv goes straight to exec — no shell ever
# sees it. The host writes JSON-RPC frames to stdin.

[[variants]]
suffix = "gpt-5.6-sol_low"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-sol\"", "-c", "model_reasoning_effort=\"low\""]

[[variants]]
suffix = "gpt-5.6-sol_medium"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-sol\"", "-c", "model_reasoning_effort=\"medium\""]

[[variants]]
suffix = "gpt-5.6-sol_high"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-sol\"", "-c", "model_reasoning_effort=\"high\""]

[[variants]]
suffix = "gpt-5.6-sol_xhigh"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-sol\"", "-c", "model_reasoning_effort=\"xhigh\""]

[[variants]]
suffix = "gpt-5.6-sol_max"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-sol\"", "-c", "model_reasoning_effort=\"max\""]

[[variants]]
suffix = "gpt-5.6-terra_low"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-terra\"", "-c", "model_reasoning_effort=\"low\""]

[[variants]]
suffix = "gpt-5.6-terra_medium"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-terra\"", "-c", "model_reasoning_effort=\"medium\""]

[[variants]]
suffix = "gpt-5.6-terra_high"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-terra\"", "-c", "model_reasoning_effort=\"high\""]

[[variants]]
suffix = "gpt-5.6-terra_xhigh"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-terra\"", "-c", "model_reasoning_effort=\"xhigh\""]

[[variants]]
suffix = "gpt-5.6-terra_max"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-terra\"", "-c", "model_reasoning_effort=\"max\""]

[[variants]]
suffix = "gpt-5.6-luna_low"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-luna\"", "-c", "model_reasoning_effort=\"low\""]

[[variants]]
suffix = "gpt-5.6-luna_medium"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-luna\"", "-c", "model_reasoning_effort=\"medium\""]

[[variants]]
suffix = "gpt-5.6-luna_high"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-luna\"", "-c", "model_reasoning_effort=\"high\""]

[[variants]]
suffix = "gpt-5.6-luna_xhigh"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-luna\"", "-c", "model_reasoning_effort=\"xhigh\""]

[[variants]]
suffix = "gpt-5.6-luna_max"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.6-luna\"", "-c", "model_reasoning_effort=\"max\""]

[[variants]]
suffix = "gpt-5.5_low"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.5\"", "-c", "model_reasoning_effort=\"low\""]

[[variants]]
suffix = "gpt-5.5_medium"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.5\"", "-c", "model_reasoning_effort=\"medium\""]

[[variants]]
suffix = "gpt-5.5_high"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.5\"", "-c", "model_reasoning_effort=\"high\""]

[[variants]]
suffix = "gpt-5.5_xhigh"
args = ["app-server", "-c", "sandbox_mode=\"workspace-write\"", "-c", "model=\"gpt-5.5\"", "-c", "model_reasoning_effort=\"xhigh\""]
